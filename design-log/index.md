# Design Log Index

Catalogue of design logs in order. New entries append to the bottom.

**Status values:** `Draft` · `Approved` · `In Progress` · `Implemented` · `Superseded`

| # | Title | Date | Status | Description |
| ----- | --------------------------------------------------- | ------------ | ------------- | -------------------------------------------------------------------------------------- |
| 001 | [Progress Projection](001-progress-projection.md) | 2026-05-17 | Implemented | Two counter layers (logical / wire), three speed flavours, projection as pure picker. |
| 002 | [GUI Reset](002-gui-reset.md) | 2026-05-17 | Superseded | Browser CSS reset + tokens + bundled Inter + shared primitives. Superseded by 015 (tokens + primitives shipped under Departure Mono brand). |
| 003 | [Encoder Pool](003-encoder-pool.md) | 2026-05-18 | Implemented | Replace single zstd encoder + mutex with sync.Pool mirror of decoder side; unblocks pull fan-out. |
| 004 | [R2 Retry Decorator](004-r2-retry-decorator.md) | 2026-05-18 | Implemented | RetryingStorage decorator + RangeGetter capability for mid-stream resume; fix GetStream error attribution. |
| 005 | [Storybook Harness](005-storybook-harness.md) | 2026-05-20 | Implemented | Storybook (web-components-vite) reuses real `wails-api` via `setTransport` interception; lifecycle driven by app buttons. |
| 006 | [Log Delivery Latency](006-log-delivery-latency.md) | 2026-05-20 | Superseded | Coalesce GUI log events on Go side (ring + 16ms flush) + rAF append on frontend; kill minutes-long post-burst spam. Superseded by [042](042-server-logs-console.md): its idle-quiescent batching ring (`cmd/gui/main.go` `batchingLogEmitter`) shipped there + the storage-fan-out burst is now off the wire. |
| 007 | [HIG UX Coherence: one dial](007-hig-ux-coherence.md) | 2026-05-20 | In Progress | Steam × macOS Software Update metaphor: single morphing circular dial replaces five stage layouts; IDLE→PREP→RUN→FINAL loop with color tokens; LOCKED+FAILED as overlays; no banners; settings behind ⚙ gear. Bulk shipped via [013](013-dialed-gui-cutover.md) cutover + [017](017-stage-bucket-honesty.md); remaining gap (2026-05-29): advanced settings is a text "Advanced" disclosure, not a ⚙ gear icon. |
| 008 | [Decoder v2](008-decoder-v2.md) | 2026-05-21 | Implemented | Composable decoder text: SOLID-split engine (Rng/GlyphSource/Cell/Ripple/Scheduler) + thin Lit element; both-way "stone-in-water" ripple as single primitive; seedable PRNG; configurable splash; whitespace inert. |
| 009 | [Telemetry hierarchy](009-telemetry-hierarchy.md) | 2026-05-21 | Implemented | ETA in dial `sub` (plain when digits, decoder-jitter on `·····` placeholder); under-block speed+size column with digit-presence rule — units / `/` / rush-placeholders decoded, digits plain; `<stable-num>` primitive anchors volatile slots; canonical 007 stage labels; no filenames. VM wiring live in `ritual-app.ts`. |
| 010 | [RUN-stage addresses](010-run-addresses.md) | 2026-05-21 | Implemented | RUN-only `<run-addresses>` component fills under-block slot when telemetry hides; all reachable IPs as rows; whole row is copy hit target with HIG feedback; decoder-v2 on identity reveal + slow idle; four-layer copy event (breath + icon swap + address highlight + GSAP micro-bounce). |
| 011 | [Dial frame FLIP](011-dial-frame-flip.md) | 2026-05-21 | Superseded | FLIP wiring produced inverted motion; first pivot (top-anchor cycle) stuck dial to viewport top; final pivot reserves `.frame { min-height: 480px }` covering worst-case stage so centred cycle never re-centres and dial position is constant across all stages. No animation needed. |
| 012 | [Drop decoder v1, unbrand v2](012-decoder-drop-v1.md) | 2026-05-21 | Implemented | Delete `decoder-text` (v1) + its lone story; rename `decoder-v2/` → `decoder/`, tag `decoder-v2` → `rune-decoder`; all live callers already on v2. |
| 013 | [Dialed GUI cutover](013-dialed-gui-cutover.md) | 2026-05-21 | Implemented | Execute #007 Phase C: `ritual-app.ts` renders `<ritual-dial>` + telemetry/addresses under-slot directly; delete `frontend/src/stages/` + `error-banner`; lifts `dial-composition` story shape into production. |
| 014 | [Prep Advanced Settings](014-prep-advanced-settings.md) | 2026-05-23 | Implemented | IDLE-only `<details>` disclosure restores port + memory inputs from old `stage-idle`; HIG inline hints, persistence on Start, no popovers, no JS open-state. Phase A `GetPrep` + Phase B `<prep-settings>` + Phase C ritual-app wire all shipped via 015 primitives. |
| 015 | [Design system](015-design-system.md) | 2026-05-23 | Implemented | Audit-driven rune-* primitive layer over existing tokens; HIG-anchored; @lit/context for wails-api + dialState; @web/test-runner posture; supersedes 002 primitives portion; unblocks 014. 6 primitives + tokens story + 34 tests; component migrations (addresses/dial) and context consumer sweep deferred as follow-ups. |
| 016 | [Live Sync Resurrected](016-live-sync-resurrected.md) | 2026-05-23 | Implemented | Resurrect 5-min save+commit+push tick as a `livesync` subsystem; single-draft-per-session invariant via existing `refs.Committer{Amend}`; shutdown preempts via `Drain` barrier; explicit case matrix for crash/lock-loss/offline flows. Supersedes ripped-out v1 (`ab380bc`). |
| 017 | [Stage bucket honesty](017-stage-bucket-honesty.md) | 2026-05-25 | Implemented | New `Phase` field collapses 8 runtime stages into 8 user-meaningful phases (idle/locked/downloading/preparing/playing/wrapping/saving/failed); ServerReady gates run bucket, ServerStopping gates final bucket; ETA only during bytes-flowing beats; brain-cog + "Preparing…" and unplug + "Wrapping up…" replace mid-prep/final dishonesty; retry-from-failed cut, replaced by dismiss-to-idle (Apple alert-pattern). Supersedes 007 §State table FAIL + PREP/FINAL glyph rows. |
| 018 | [Logical rate in the UI](018-logical-rate-in-ui.md) | 2026-05-25 | Implemented | Under-slot readout + ETA consume `logicalMbps` (decompress rate) instead of `speedMbps` (wire rate) so units match the logical bytes counter beside them; wire field stays on ViewModel for logs + future dual-series chart. Refines 001 §Decision 2. |
| 019 | [PlanInfo announces the delta, not the ref total](019-plan-info-delta.md) | 2026-05-25 | Implemented | Pre-flight `dst.List("objects/")` once per Push/Pull filters items before `onPlan`; `PlanInfo.BytesTotal`/`FilesTotal` describe what will actually move, so bar fills to 100%, ETA matches wall-clock, and "X / Y GB" stops lying about a re-sync. Projection anchors `Progress=100` on empty delta; R2 `List` paginated to survive >1000 keys. |
| 020 | [Lit render purity sweep](020-lit-render-purity.md) | 2026-05-25 | Implemented | Sweep eight Lit render/lifecycle violations across nine files: scroll-on-every-update bug, render reads of wall-clock + non-`@state` fields, `willUpdate` layout reads, GSAP-past-the-setter trick, identity-churn array literals, light-DOM reads in render, entrance animations on empty first frame. Single-derivation `speedBps` `@state` in `ritual-app`; `@slotchange` flags in `rune-field` / `rune-sheet`; module-const splash arrays. |
| 021 | [Sync Upstream](021-sync-upstream.md) | 2026-05-25 | Superseded | Superseded by [031](031-bidirectional-sync.md). Proposed an IDLE-only single "Sync upstream" force-push via a new `probing.Strategy`. Two findings killed it: refs are self-contained ⇒ `Parent` is inert ⇒ no head-resolve stage needed (Probing solves a non-problem), and a single local-only direction doesn't cover "get latest". Replaced by two directional buttons over existing stages. |
| 022 | [Divergence from origin/feat/delta-sync](022-divergence-from-origin-delta-sync.md) | 2026-05-25 | Decided | Meta-log of a branching decision: local + origin/feat/delta-sync forked at `bc1c547` (35 commits local vs 5 origin); merge attempt broke the build because origin's `cmd/gui/main.go` calls APIs (`pusher.OnItemDone`, expanded `projection.New`) local doesn't expose. Decision: stay on local timeline; surgical back-ports for the four genuine wins (`4e010eb` perf live-set, `a7949ae` honest push + heartbeat, `9fdf1f6` Apply/Retain `ItemDoneInfo`, `76c0682` R2 dotenv); skip ETA/Applying caption (superseded by #018/#017) and eventbus blocking subs (needs #016 re-validation). |
| 023 | [Disable main-window resize](023-disable-resize.md) | 2026-05-25 | Implemented | Lock the main window to 560×720: `DisableResize: true` + `MaximiseButtonState: ButtonHidden`, drop dead `MinWidth/MinHeight`; logs window stays resizable. Prerequisite for [[024-custom-titlebar]]; protects 007/011/020 layout assumptions from runtime resize. Phase B live smoke deferred. |
| 024 | [Custom unified titlebar (exploratory)](024-custom-titlebar.md) | 2026-05-25 | Draft | Exploratory log on replacing the Windows OS chrome with a `<rune-titlebar>` Lit primitive; Mac already half-customised via `MacTitleBarHiddenInset`. Three variants (Windows-only / unified both / skip); recommend Variant B (Windows-only, Mac native — Slack/VSCode/Figma pattern). Depends on 023. |
| 025 | [Drop redundant per-blob `Exists` gate in `transferBlob`](025-drop-redundant-exists-gate.md) | 2026-05-25 | Implemented | After [[019-plan-info-delta]] added a pre-flight `dst.List("objects/")`, `transferBlob` still re-checks every missing blob with `Exists` — 2005 `storage.exists` calls in a single session, ~1m42s wasted in Pushing. Drop the gate; PutStream is content-addressed idempotent so the documented race becomes a no-op duplicate upload. Regression test asserts `existsHitsPrefix("objects/") == 0` on a clean Push. |
| 026 | [Stuck in saving phase after backend reaches Done](026-stuck-in-saving.md) | 2026-05-25 | Draft | Session log shows `Unlocking → Done` + `status: done` published cleanly, but the dial sat on "Wrapping up" at 100% for 11 minutes until manual Stop. Backend path unit-tested correct; `wailsViewEmitter` review finds no delivery hole. Committed hypothesis: `<rune-decoder>` `.text` reactivity bug — label decode completed at 100% and never re-fired on the Idle transition. **Q12 update:** user reports state eventually self-resolved → hypothesis shifts to delayed delivery (Wails IPC backpressure or WebView2 throttling), not permanent reactivity break. Phase A: fix on hypothesis; Phase B: instrumented fallback. **Verified 2026-05-29:** `<rune-decoder>` `updated()` already reacts correctly to `.text` change (`decoder/index.ts`) — no reactivity break in code; no targeted fix landed. Stays Draft (delayed-delivery hypothesis unconfirmed). |
| 027 | ["Saving worlds" copy + Prep/Wrap ETA from session history](027-saving-worlds-prep-eta.md) | 2026-05-25 | Draft | PhaseWrapping label "Spinning down" → "Saving worlds"; both PhasePreparing and PhaseWrapping get a `~Ns` sub derived from trimmed mean of last-10-session durations. New `internal/subsystems/preprundup/` records prepMs (Acquiring→ServerReady) and wrapMs (ServerStopping→Done) into `<root>/prep-history.json`; `Estimator` feeds new `PrepEtaMs`/`WrapEtaMs` ViewModel fields. First-run fallback: "Almost live" / "Almost done." |
| 028 | [Transfer ETA stability (kill the swing)](028-transfer-eta-stability.md) | 2026-05-25 | Implemented | Download/upload ETA swings 4min→30s→2min inside 3 seconds because it's derived from a 5s rolling mean of a fundamentally jittery link. Switch ETA to **beat-wide average** computed Go-side + monotonic guard so it only decreases within a fixed plan. Shipped entirely in `projection` (deviation from §Q8: no `Stream` change — `Tick.Elapsed` + projection state suffice); new `vm.EtaSeconds`, frontend drops the division. Speed cell left on `DataAverage` (§Q9 deferred — `Smooth` would regress [[018]]). Hybrid weighting + Phase D smoke deferred. |
| 029 | [Advanced disclosure clipping below viewport](029-advanced-disclosure-position.md) | 2026-05-25 | Implemented | `<prep-settings>` from [[014]] overflows the 560×720 fixed window ([[023]]) when expanded — total stack ~836px vs 720px budget. Phase A: IDLE-only `padding-top: 60px` on `.stage` (was 150px) recovers 90px, animates via `--motion-base` on transition. Phase B (conditional dial-frame trim) and Phase C storybook story deferred. |
| 030 | [R2 as default backend + runtime `.env` loader](030-r2-default-and-dotenv.md) | 2026-05-25 | Implemented | Replace compile-time `const remoteMode = ModeMock` with runtime `RITUAL_REMOTE_MODE` (default R2, "mock" opt-in). `config.LoadEnvFiles()` reads `.env.{$RITUAL_ENV}.local` (default `dev`) via `joho/godotenv` from exe-adjacent + CWD; mirrors Taskfile's `R2_*` → `RITUAL_R2_*` remap. Shell env always wins. Deviation from §Q5: file naming aligned to shipped Taskfile (`{dev,prod}` variants) rather than `.env.local`/`.env` — one convention across `task` and shipped binary. |
| 031 | [Bidirectional Sync (Download / Upload + launch staleness)](031-bidirectional-sync.md) | 2026-05-29 | Implemented | Supersedes [021](021-sync-upstream.md). Two directional flows over **shared stage nodes** ((A) separate builders) + a passive launch staleness check. Download = `Checking → Pulling → Retain(local)` (no lock, no push). Upload = `Checking → Probing → Acquiring → Committing → Pushing → Retain(L) → Retain(R) → Unlock`; `Probing` (head-only Pulling) sets `rs.ParentRefID` so the **unchanged** resolver records `Parent = remote HEAD` (lineage chosen for future history). Not one button: `Apply` prunes local-only in-scope files. Staleness = control query `GetSyncStatus` (boolean `behind` = remoteHEAD > localHEAD timestamp; no seq-id count — deferred). UI: quiet dial `sub` caption + gestures in collapsed Advanced, **no primary Download** (session already pulls). `lifecycle.Entries{Session,Download,Upload}` routing. |
| 032 | — (no file) | — | Implemented | **Dangling reference, not a real log.** `CLAUDE.md §On Tool Descriptions` cites `design-log/032 §Rule` (R1''' headline + R2' arg micro-template + R3 wire-detail-in-handler), but no `032-*.md` ever existed here — numbering skips 031→033. Carried over from another project; the cited rule does not apply to this repo. Slot left empty intentionally. Remove/rewrite the CLAUDE.md citation when tool-description conventions are actually defined here. |
| 033 | [Retention Rules UI (Borg-style tier picker + survival visualization)](033-retention-ui.md) | 2026-05-30 | Implemented | Storybook UI for the tiered retention engine: `rune-segmented` primitive (0–5 per tier) + `retention-rules` component with a computed kept-vs-pruned timeline (faithful TS port of the Go `Mark` union). Built + wired end-to-end via [039](039-retention-control-plane.md). |
| 034 | [Immersive View Stack (push/pop navigation, no modals)](034-immersive-view-stack.md) | 2026-05-30 | Implemented | `rune-stack` navigation primitive: root = default slot, pushing a view slides the whole screen left (`translateX(-index·100%)`), `←` bar pops it back. Arbitrary depth; **lazy-mounted panes** (push instantiates, pop unmounts after the slide settles via `transitionend`); `NavController` provided through `@lit/context` so any descendant pushes deeper without prop-drilling. The modal-free, HIG-aligned replacement for the dialogs/popups/toasts the user rejected; sync [031] + retention [033] become tenant views. Phase A (primitive + 3 stories + 8 tests) **and** Phase B (app integration: `advanced-view` + `sync-view`, wired in `ritual-app.ts`, 64 tests) shipped (commit 23d1a16); Phase C (retention tenant) deferred pending [033] migration. |
| 035 | [Create version (make local state canonical)](035-publish-local-changes.md) | 2026-05-30 | Implemented | **Governing principle: user data is sacred — preserve it, even retrospectively.** Reframes [031]'s "Upload" as **ref creation / recovery**: snapshot the current workdir as a new ref that becomes HEAD, **parented on local HEAD** (not remote — reverses 031 §Q1) so a rolled-back state is made canonical truthfully. Offered whenever local work isn't safely canonical: **dirty** (workdir ≠ local HEAD, `MtimeScanner` over `ref.Objects`) **or unpushed** (local HEAD ahead of remote — a crashed normal-session push; one tap pushes the existing ref). A [036] skip-sync session surfaces as **dirty** (it writes no ref); the castle-on-a-plane recovers via that dirty path. **Failsafe, never blocked**; when `Behind`, a non-blocking "Remote is newer" warning, never a gate. Reuses 031's pipeline (swap `probing` resolver remote→local, always-commit). Boolean offer, no file count. User-facing verb **"Publish"** (parallel to Download; honest across dirty *and* unpushed, where "Create version" lies); IDLE cue **"Unpublished changes"** below Advanced; data-sacred framing in the confirm body. Lives in Advanced → Sync. When dirty+behind, Publish primary / Download secondary (preserve-first). |
| 036 | [Skip sync this session (local-only launch)](036-skip-sync-session.md) | 2026-05-30 | Implemented | Explicit per-session "Skip sync this session" toggle (no auto-detect, transient, default OFF) that launches the server with **all sync skipped** — for offline / R2-down / rollback / mod-testing / "just check stuff". A fourth separate-builder graph `BuildLocalSession` (031 §Q-arch pattern, shared stage constructors): chain is **`Checking → Running → Done`** — **saves nothing** ("skip sync means we won't save either"), no Pulling/Acquiring/Committing/Pushing/Retain/Unlock/livesync. Durability is the Minecraft server's own disk autosave; the work lands in the workdir and is recovered **deliberately** afterward via [035] **Publish** (it reads **dirty**, workdir ≠ HEAD). Sharp edge: pressing the dial before Publishing pulls-and-overwrites — informed consequence, prompted by the "Unpublished changes" cue. `StartRequested.SkipSync` routes to `Entries.LocalSession`; skipping is structural (nodes absent). Discovery: explicit toggle in Advanced→Settings (intent) + a FAILED-view "Skip sync & run locally" hint on sync-stage failures. Running shows a muted "Local only" caption. |
| 037 | [Autoupdate as a Preflight dial stage](037-autoupdate.md) | 2026-05-30 | Implemented | Restore the pre-2.0 self-update lost in `046045a` (deleted as collateral with the v1 manifest tail), reshaped for the Wails GUI. **Autoupdate becomes a new leading state-machine stage `Preflight`, not a notification** (no toast/banner/modal, per 007/034): every launch the dial boots **gray + inert** ("Checking for updates···"), then wakes to IDLE (up-to-date) or morphs **gray ring-fill "Updating → vN" → "Restarting···" → relaunch** (outdated). Update is **mandatory but best-effort** — failure routes into the existing `PhaseFailed` (017), informs retry (restart / Advanced ▸ Check for update), and drops to a usable IDLE; shares the FAILED affordance + offline escape with [[036-skip-sync-session]]. Manual "Check for update" in Advanced = full Preflight takeover (one path). Backend: `internal/subsystems/selfupdate` + resurrected `IsVersionOlder`; **`minio/selfupdate`** does the atomic running-exe rename+rollback (collapses the old 3-process `--replace-old`/`--cleanup-update` dance); relaunch = `exec` + `wailsApp.Quit()` (Wails v3 has no restart API). Version source (Q7 resolved 2026-06-03) = **no feed file**: listing-derived latest over `bin/<os-arch>/<version>/<sha>` in R2 (hash in the key name = self-verifying; semver-max via `IsVersionOlder`, mirrors `HeadResolver`). Publish = `cmd/publish` Go tool through the app's own R2 adapter via `task publish` (version from `config.AppVersion` const → anti-loop invariant). **Fully observable**: `observed.NewUpdater` decorator + observed `remoteStorage` + `Update*` bus events → the dial and the single `<root>/logs/<ts>.log` consume one event stream. macOS out of scope; Windows primary. Open: Q9 portable-vs-installed (client elevation; not a publish blocker). |
| 038 | [Restore to a previous version (world-save rollback)](038-restore-previous-version.md) | 2026-06-04 | Implemented | Pick a historical ref and restore the workdir to it. New `ListVersions` + `Restore` control API, `RestoreRequested` command + `BuildRestore` pipeline (`Checking → Pulling(target) → Done`, read-only on remote — no lock/push), and a target-pinned pulling resolver (`FromHead`/`FromTarget` over a new `RunState.TargetRefID`). Non-destructive: HEAD never moves, no ref deleted; restored state surfaces as **dirty** and recovers canonically via [035] Publish (`Parent = local HEAD`). Reuses the download dial beat (`FlowRestore`). `versions-view` section in Advanced. World saves only — not the app binary ([037] owns that). |
| 039 | [Retention control plane (editable rules in the GUI)](039-retention-control-plane.md) | 2026-06-04 | Implemented | Make the live retention engine controllable. Rules read **fresh from `settings.json` at prune time** (scope-driven `NewRefsRetention(storage, ScopeLocal/Remote)`; `Select` does the read) — no closures, no startup capture, so edits apply without a restart (user directive). `Get/SetRetentionRules` control API (0–5, keep_last:0 allowed). Builds the [033] UI (`rune-segmented`, `retention-model`, `retention-rules`) + a one-panel scope-switch (Local · R2) Retention section in Advanced; timeline fed real history via [038] `ListVersions`. Logs tier stays fixed; presets deferred. |
| 040 | [Lazy directory creation (no empty folders on boot)](040-lazy-directory-creation.md) | 2026-06-04 | Implemented | Stop eagerly `MkdirAll`-ing `server/` (+ `remote-mock/`) at boot; directories materialise on write like `refs/`/`objects/` already do in `PutStream`. Root `MkdirAll` survives as the documented exception (sandbox anchor for `os.OpenRoot`, never empty — `logs/` lands immediately). Driver: `os.OpenRoot` requires the dir to pre-exist, so eager creates are a side-effect of opening sandbox roots, not deliberate layout seeding. Recommend a lazy server-root opened on first launch (by which point Apply created it). User directive: 0 reason for empty folders. |
| 041 | [Work folder selection (movable files root, fixed operational root)](041-work-folder-selection.md) | 2026-06-04 | Superseded | Advanced **Open folder** / **Change folder**, split by weight into operational path (fixed, `settings.json` + a new `data_root` field) vs files path (`DataRoot`, user-selectable). Left unedited — logs are immutable. Superseded by [[055-operational-functional-root-split]]: same move mechanics (rename fast path, `EXDEV` copy+verify+delete) carried over, but re-split by purpose (OPERATIONAL vs FUNCTIONAL) after finding 041's list dropped `worlds/` and never addressed `lock` or the second (`server/logs/`) log directory. |
| 042 | [Server logs console (MC-only wire, tail-follow UI, Storybook)](042-server-logs-console.md) | 2026-06-04 | Implemented | Narrow the logs window from a whole-bus mirror to **MC server console only** (`logsink` allowlist: `ServerOutputInfo` + `ServerCrashedInfo` + new `ConsoleEchoInfo`; full bus stream stays in `<root>/logs/<ts>.log` unchanged). Rename wire `log:line`→`server:logs` / `LogLine`→`ServerLog{Kind,Level,Text}`, batched. **Build [[006-log-delivery-latency]]'s idle-quiescent batching ring now** (lazy timer, zero wakeups at rest — "no DoS when no requests"); frontend drops rAF and appends batches directly. UI rework kills the scroll hijack (`updated()` force-scroll) via **CSS `column-reverse` tail-follow** (no JS `scrollTop`) + `IntersectionObserver` "Jump to latest" pill; raw monospace rows (drop our ts/level chrome — MC carries its own), frontend substring WARN/ERROR tint; **backend-driven** input echo via `ConsoleEchoInfo` on confirmed stdin write (delete `console input:` prefix coupling — UI is 100% wire-driven). Adds the missing `ritual-logs.stories.ts` (5 stories). Supersedes 006 (its storage-fan-out burst is now off the wire). |
| 043 | [Server logs: RUN-stage-only access + lazy on-demand backfill](043-server-logs-runstage-only.md) | 2026-06-05 | Implemented | Remove the **global** logs affordance (`ambient-footer` `log` button — keep `folder`); expose the console **only** via a subtle RUN-stage clickyclacky mirroring [[010-run-addresses]] (faint console row under the addresses, `PhasePlaying`-gated, click→`ShowLogs`). **Lazy logs** (user directive — no eager windows, no retained memory): drop the startup hidden window; build it on first open. Backfill via **one on-demand raw read** of MC's `<server-cwd>/logs/latest.log` (cwd resolved from `settings.StartScript`; read top-to-bottom, **no parsing** — 042 already renders raw rows), then continue live on the `server:logs` wire. Flow: subscribe→hold → `ReadServerLog()` (last 1024 lines = `RING_CAPACITY`) → render → cross-ref drop dup tail → live. No in-memory ring; seam deduped by exact line-text match (same stdout). |
| 044 | [Untruthy IDLE state (stale cue, mismatched "current", forgotten checkbox)](044-untruthy-idle-state.md) | 2026-06-05 | Implemented | Three independent untruthful-state bugs on the IDLE pane, one root cause — we treat one-shot reads as truth and never reconcile after the user acts. **A. Stale cue:** `refreshUnpublished()` only fires in `connectedCallback`; the "Unpublished changes" cue sticks after a livesync push and Wrapping→Idle. Fix: phase-edge re-probe in `applyVm` on `!wasIdle && isIdle`. **B. "current" badge points at HEAD, not what's loaded:** `IsHead = newest ref` is correct for HEAD; the badge **label** is the lie because Restore moves the workdir without moving HEAD ([[038]]). Proposed: new `settings.loaded_ref_id` written on Restore-Done / Pulling-Done / Publish-Done; `Version.IsLoaded` drives the badge (fallback to `IsHead` when unset). **C. Skip-sync checkbox forgets across Advanced reopens:** [[034]] lazy-mount re-instantiates `<prep-settings>` and its local `@state skipSync` resets to OFF; host already mirrors the bool but never feeds it back. Fix: hoist to `@property`, host stays authoritative, per-launch reset preserved ([[036]] §Q6 unchanged). |
| 045 | [Versions & retention ops (delete, tabs, revert, apply-now, total-on-disk)](045-versions-and-retention-ops.md) | 2026-06-05 | Implemented | Five contained gaps in Advanced under one log. **A. Delete local version:** new `DeleteLocalVersion(refID)` = `localStorage.Delete(refs/<id>.json)` + `refs.Collector.Collect` (GC orphans); UI affordance hidden on the loaded row; copy notes the remote keeps the canonical copy. **B. Local · Remote tabs:** `rune-segmented` at the top of `<versions-view>` ([[033]]/[[039]] pattern); Local-tab adds Delete + the on-disk header. **C. Revert to last saved (Sync):** new `RevertRequested` + `BuildRevert` = `Checking → Pulling(FromLocalHead) → Done`; appears only when `dirty == true`, secondary to Publish; HEAD never moves. **D. Retention Apply:** staged-edit rework of `<retention-rules>` (proposed Q6b — replaces auto-save with explicit Apply/Discard); Apply persists settings AND publishes `ApplyRetentionRequested` → new `BuildRetentionRun` = `Checking → Retaining(local) → Retaining(remote) → Done` (`PhasePreflight` gray ring reused); rejects while Running. **E. Total-on-disk (dedup-aware):** new `GetLocalStorageStats()` walks local `objects/` for bytes+count (5s cache, invalidate on Delete/Apply); Local-tab header *"N versions · X.YY GB on disk"* + soft dedup hint when `Σ row.sizeBytes > 1.5× bytesOnDisk`. Open Qs: auto-save vs staged retention (Q6a/b — proposed b), Delete-on-loaded-row combo (OQ1), Revert confirm copy (OQ5). |
| 046 | [Hash the diff, not the tree (change-proportional apply & commit)](046-diff-driven-apply-commit.md) | 2026-06-05 | Draft | Stop re-hashing the whole workdir on every apply/commit. Measured idle restart (`logs/20260605153656.log`, `plan pull files=0`): apply skip-gate re-reads **1.2 GB / 2456 ms** to conclude "nothing changed", prune re-hashes the **same tree again** for hashes it never reads (only keys — `apply.go:160`), commit's `FullScanner` hashes everything. Principle: **hash bytes when they move, never at rest** — in-motion verify (write-time xxhash §822, blob read-verify §820) untouched. **(1)** Prune hash-free (path-only walk; spec step 4 §1059 is path-membership — zero ACID impact). **(2)** Commit swaps `FullScanner`→`MtimeScanner` (`since=last-commit`; the [[035]] dirty-prober's existing gate). **(3)** Apply = manifest `diff(X,Y)` (pure metadata) + write+verify only Δ, trusting an **applied-head marker** (`settings.loaded_ref_id` [[044]] + new `synced_at`); **two anchors not one** — `last-commit-time` (commit) vs `last-sync-time` (apply): apply *writes* files so `mtime=apply-time` defeats `since=ref-timestamp` (the load-bearing trap). Fallback to today's full re-hash when marker absent/stale (`control.go:142` sentinel precedent). Amends Integrity Model: the every-apply full re-hash also incidentally scrubbed at-rest workdir rot — **dropped** ([[025]] precedent; canonical data is content-addressed + lives on R2, mtime-commit won't launder rot into history, MC server surfaces broken files, recovery = re-Download). No design log ever proposed workdir verification; the only sample-verify (spec §826) is blob-scoped, `[should-have]`, unimplemented — earlier draft mis-cited it, corrected. Q4 racy-mtime guard (size co-check + treat `mtime==since` as dirty — prevents `commit.go:212` silent data-loss) hardens all 3 mtime consumers. Gain: idle 2.4 GB→~0, I/O O(tree)→O(change), conflict-detection falls out. **Decided (user 2026-06-05):** dirty+remote-moved = steamroll + emit conflict signal; sample-verify dropped. Open: hash-free scanner shape (OQ4). |
| 047 | [OS notifications on critical lifecycle moments](047-os-notifications.md) | 2026-06-05 | Implemented | Native OS toasts for the **criticals only** (user directive): server **up** (`running.ServerReadyInfo`), clean **stop** (`StatusChanged{Done}` gated on a `sawReady` latch so non-server flows stay silent), and **failure** (`StatusChanged{Failed}` + `Err` in body). Uses Wails v3's vendored `pkg/services/notifications` — **no perms on Windows** (service self-registers AppUserModelID/CLSID under HKCU on `Startup`; only user Focus-Assist suppresses), **fully backend-side** (webview has no notification/system-info API anyway). New `internal/subsystems/notify` = narrow `Notifier` port + bus-consumer (`Attach`, one goroutine, per-event `Notify` dispatched detached so a slow toast never stalls the bus — mirrors `wailsViewEmitter`); thin `wailsNotifier` adapter + `Services` registration in `cmd/gui`. ID = `ritual-<kind>-<runSeq>` (`runSeq++` per `FlowStartedInfo`, clockless/test-friendly). Open Qs: notify on **all** flow failures vs server-only (Q3, proposed all); focus suppression (Q5, proposed none). |
| 048 | [Local-storage build variant (`gui:build:dev:local`)](048-local-storage-build-variant.md) | 2026-06-18 | Draft | New build-time backend axis: `gui:build:dev:local` bakes `RITUAL_REMOTE_MODE=mock` (new `remote.bakedRemoteMode` ldflag, env still wins) → `bin/ritualdev-local.exe`, local-FS only, for testing the full 037/038 update lifecycle offline (incl. installed binary). Shared `config.AppName=ritualdev` so the GUI and `go run ./cmd/publish` align on one `~/k10wl/ritualdev/remote-mock`; distinct filename so both backends coexist. Adds `gui:run:dev:{local,remote}` + `publish:dev:local`; `prod` stays remote-only; `gui:build:dev` aliases `:remote`. Surfaces the backend in the task name per the no-hidden-flag rule. |
| 049 | [CI publish workflow (GitHub Actions)](049-ci-publish-workflow.md) | 2026-07-18 | Draft | New `.github/workflows/publish.yml`, two jobs on `push: [dev, main]`: `dev` push runs `task publish:dev:remote` under a `development` GitHub Environment; `main` push runs `task publish:prod` under a `production` Environment. Both Environments carry a required-reviewer approval gate (manual repo Settings, not YAML). `windows-latest`, same toolchain as README (Go/Node/Task/Wails3 CLI + golangci-lint installed via `install-only`). A step writes `.env.{dev,prod}.local` from environment-scoped R2 secrets (`R2_BUCKET_NAME`/`R2_ACCOUNT_ID`/`R2_ACCESS_KEY_ID`/`R2_SECRET_ACCESS_KEY`, matching `.env.example`) immediately before `task publish:*`, since Task's `dotenv:` requires the file to exist and `_publish` already gates on `test`+`lint` before touching R2. New long-lived `dev` branch created from `origin/main`. `ci.yml` (PR test loop) unchanged, kept separate. |
| 050 | [Chained-launch progress overflow, dial-icon test coverage gap + backend-driven telemetry](050-chained-launch-progress-overflow-and-missing-icons.md) | 2026-07-19 | Implemented (§A, §C) / Hardened, unconfirmed (§B) | **§A implemented:** `internal/adapters/counter.go` `StorageCounters` are process-lifetime cumulative atomics (constructed once in `cmd/gui/main.go`, never reset), but `onTick` was assigning their raw value straight to `ViewModel.BytesDone` against a freshly-scoped per-flow `PlanInfo.BytesTotal` — so a second pull/push inside one running process (chained dial restarts) opened with a stale numerator, producing >100% readouts (log: `done=1280889536/61655570`, 2078%). Fixed: `Projection` now baselines `BytesDone`/push `Ops.Done` against the raw counter value observed at `Checking→Pulling`/`Pushing` stage-entry; TDD tests (`TestProjection_Chained{Pull,Push}_BytesDoneResetsPerFlow`) reproduce the exact logged byte counts and pass. **§B narrowed + hardened:** requestor identified the actual report as the dial's radial-button glyphs, not the exe icon — root risk (`lucide`→SVG-path conversion silently returning `""` for an unhandled shape tag) had zero test coverage because **no frontend test was ever wired into `task test`**; fixed structurally (`common:test:frontend` now gates every `task publish:*`). Not confirmed against the friend's actual machine. **§C (new, same session): "client is only a projection."** Walking through where the displayed number comes from surfaced a frontend that independently computed unit-formatted text, a **client-side fallback rate estimate**, and ran its own `setInterval` clock for the "Live" uptime caption — all now backend-owned: `ViewModel` gains `SizeDoneText`/`SizeTotalText`/`SizeUnit`/`SpeedText`/`SpeedUnit` (Go-side `formatSize`/`formatSpeed`, ported from the deleted `telemetry-format.ts` equivalents) and `UptimeSeconds` (driven by a new 1Hz ticker in `Projection.Run` while `PhasePlaying`, so the frontend runs no clock of its own); the client-side speed-estimate fallback is deleted outright, not fixed, per the user's "duplicated hallucinated calculations" objection. Percentage/ratio math and ETA-duration copy stay frontend by explicit scope decision. Ships as a real release — `config.AppVersion` 2.0.1 → 2.0.2 — so the mandatory autoupdate flow ([[037-autoupdate]]) carries it to installed clients. `golangci-lint` clean repo-wide; live-boot smoke test confirms the version bump end-to-end. |
| 051 | [GUI freeze on save: wailsViewEmitter loses the terminal Idle snapshot](051-view-emitter-terminal-freeze.md) | 2026-07-19 | Draft | Supersedes the hypothesis trail in [[026-stuck-in-saving]] (left unedited — logs are immutable) via a live repro: session log + DevTools (`document.querySelector('ritual-app').vm`) prove the frozen dial's Lit state was never updated — the terminal Idle `ViewModel` was computed and reached the file-log subscriber fine, but never reached `wailsViewEmitter` → `a.Event.Emit("ritual:view", …)` (`cmd/gui/main.go:737-774`). Ruled out: frontend rendering/`rune-decoder` reactivity (026's original hypothesis C — moot, `vm` itself never changed), bus drop, and a new per-file-volume/"bombarding the frontend" theory (falsified — `fold()` only emits on the 1Hz `progress.Tick` + coarse stage transitions, never on per-file storage events, so `ritual:view` is already rate-limited to ~1/s regardless of file count). Leading hypothesis: `wailsViewEmitter.loop()`'s single worker goroutine gets permanently wedged on one blocking `a.Event.Emit` call (no timeout/recovery today) — matches the non-self-recovering 26+ minute freeze; confirmed architecturally via a standalone Go reproduction of the same concurrency shape (scratchpad, not committed) showing a blocked emit call silently swallows all later updates forever. **2nd live repro via [[053]]:** with the window still frozen, a real RPC call (`Control.GetSnapshot()`, the Call/request-response transport) returned correctly in 6ms on the same window/moment — proving it's not a general IPC/WebView2 stall, isolating the fault to the push-event (`Event.Emit`) path specifically. **3rd live repro:** attached `dlv` live before reproducing; on freeze, a full goroutine dump of all 26 goroutines showed *nothing* blocked anywhere — `wailsViewEmitter.loop` had already returned from its emit call. Manually invoking `window._wails.dispatchWailsEvent(...)` with fabricated idle data from outside instantly fixed the frozen dial, proving the JS listener side is healthy too. **4th live repro — exact mechanism now directly proven, not inferred**: a permanent Go+browser IP-echo instrumentation (both wired via [[053]]) captured the terminal sequence on both sides byte-for-byte — Go submitted `saving, saving, saving, saving, idle` in that exact order (all confirmed returned), but the browser *executed* them as `saving, saving, saving, idle, saving` — the last stale "saving" duplicate finished executing **after** idle and overwrote it, because the frontend applies whatever arrives last with no ordering check. Root cause: `ExecuteScript(script, nil)` (`go-webview2@v1.0.23`) is fire-and-forget and does not preserve submission order as execution order under load — not a deadlock, not a full loss, a causal-ordering race. **Implemented and confirmed live**: `ViewModel.Seq` stamped once per emit by a new `Projection.emit()` (also fixed a pre-existing unsynchronized `Snapshot()`-vs-`Run()` data race with an `RWMutex`, caught mid-implementation); frontend's `isNewerSnapshot` (new `vm-seq.ts`) drops/warns on any non-newer snapshot. Go + frontend tests green. Five real live Upload cycles reproduced the same underlying WebView2 delivery race unchanged (dropped/out-of-order sequence numbers still occur) but it's now inert — one cycle caught the exact race live (`seq 123 idle` arrived, then stale `seq 122 saving` arrived after it and was dropped with a console warning) — dial stayed correctly on idle, no freeze. |
| 052 | [Debug REST control API (full ControlService parity, dev builds only)](052-debug-control-rest-api.md) | 2026-07-19 | Superseded | Motivated by [[051]]: reproducing GUI bugs today requires a human manually driving the real window and reading DevTools by hand — no non-GUI control surface exists. Proposed `internal/gui/debugapi`, a thin HTTP wrapper over `control.ControlService` plus an independent SSE event tap. Checked whether Wails already provides this (§Q7/Q8: it has a "server mode" but it fails to compile on Windows with our pinned version, and structurally can't see 051's bug anyway since it deletes WebView2 entirely). Superseded by [[053-cdp-webview-inspection]]: user opted to attach Chrome DevTools Protocol directly to the real WebView2 window instead of building a parallel backend-only API — never implemented. |
| 053 | [Live WebView2 inspection via Chrome DevTools Protocol](053-cdp-webview-inspection.md) | 2026-07-19 | Draft | Supersedes [[052]]. Wails v3's `application.Options.Windows.AdditionalBrowserArgs` (its own doc comment gives `"--remote-debugging-port=9222"` as the example) turns the real WebView2 window into a standard CDP target — Playwright/Puppeteer/raw-WebSocket can attach to the *actual* window the real app opens, read live Lit component state (`document.querySelector('ritual-app').vm`, exactly what manual DevTools did in [[051]]), capture console output, and drive the UI via real clicks — one mechanism for both observing and driving, in the exact code path 051 lives in (unlike 052's backend-only view, and unlike Wails' server mode which deletes WebView2 entirely). Dev-build-gated (`config.AppName == "ritualdev"`), proposed port 9222 (no collision with Vite's 9245). Open: add Playwright as a frontend devDependency vs. a dependency-free raw CDP client. Awaiting approval before implementation. |
| 054 | [PR-only CI and post-merge publish](054-pr-only-ci-and-post-merge-publish.md) | 2026-07-26 | Implemented | Move full test and lint gates to PRs; remove protected-branch push tests and post-merge test/lint gates from publish. |
| 055 | [Control vs content root split (movable workroot, fixed control root)](055-operational-functional-root-split.md) | 2026-08-10 | Implemented | Supersedes [[041-work-folder-selection]] (left unedited). Splits every root-level path by **purpose**, not weight: CONTROL (`settings.json`, `lock`, root `logs/`) stays fixed at `config.RootPath`; CONTENT (`objects/ refs/ server/ worlds/`) moves to a user-selectable `config.WorkRoot`, pointed to by a new `settings.json` field `work_root` (empty ⇒ default = `RootPath`, today's layout unchanged). No filesystem rename: `internal/core/stages/relocating` (a `machine.Strategy[State]`, matching `checking`/`pulling`'s shape) opens a new `os.Root` at the destination, copies via the existing `ports.StorageRepository` interface (`List`+`GetStream`/`PutStream`), then atomically swaps two thin `swappableStorage` facades (`rawLocalRef`/`rawWorkdirRef`) that every downstream consumer (`puller`/`applier`/`committer`/`pusher`/`locker`/etc.) is built on top of — no relaunch, no `ControlService` field changes beyond one new `workRootRefs` constructor arg. Crash safety framed explicitly around ACID (Atomicity/Consistency/Isolation/Durability), with `domain.Settings.Save()` hardened to atomic temp+fsync+rename as the sole durable commit marker; stale files left by a crash are explicitly out of scope, not cleaned up. Bus-wired via its own dedicated event vocabulary (`RelocateStarted`/`RelocatePlanned`/`RelocateProgress`/`RelocateVerifying`/`RelocateCommitting`/`RelocateFinished`/`RelocateFailed`, same pattern as `observed`'s autoupdate events) with `watchStop` cancel support; folded into `StageRelocating`/`PhaseRelocating` on the dial via an addendum. Ships API-only first (`ControlService.GetWorkRoot`/`ChangeWorkRoot`/`ResetWorkRoot`) — native folder dialog + Advanced UI deferred to a follow-up phase per user direction. **Addendum (Draft, 2026-08-11):** root `logs/` reclassified CONTROL→CONTENT per user direction — moves with the workroot instead of staying fixed. Not through `ports.StorageRepository` like the rest of CONTENT (the log sink is a single long-lived `*os.File` + goroutine, not storage-keyed); swap happens only after the relocate's durable commit point (close old file → best-effort copy historical `logs/*.log` → reopen fresh at the new root), so the relocate's own events keep logging normally throughout the copy/verify phase. Not yet implemented. |
| 056 | [Work root: native folder dialog + Advanced UI (Phase F of #055)](056-work-root-folder-dialog-ui.md) | 2026-08-11 | Implemented | Implements [[055-operational-functional-root-split]]'s deferred Phase F. Native OS folder picker + `<work-root>` Advanced UI section, wired to `ChangeWorkRoot`. Live testing surfaced and fixed 5 bugs beyond the original scope: `verify()` false-positives on legitimate empty objects/world files, a severe `cleanup()` bug that deleted the CONTROL root's settings.json/lock on a never-relocated install, dial progress freezing mid-copy of a single large file (`CounterStorage` byte tap + 500ms heartbeat), ETA frozen for 30+ seconds mid-transfer (lifetime-average estimator too insensitive over a long beat — gave `etaFromSessionAvg` a 5s sliding window), and size+ETA telemetry reusing `<dial-telemetry>`/`etaFromSessionAvg` verbatim. |
